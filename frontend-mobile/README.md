# ThreadLoop Mobile (Expo)

```bash
npm install
npm start            # then press i / a / w
```

When testing on a physical device, replace `localhost` in `app.json -> extra.apiUrl` (or via `EXPO_PUBLIC_API_URL`) with your LAN IP so the app can reach the backend container.
